@startuml
package ua-khpi-oop-lab02 {

    class Guest {
        - name : String
        - phone : String
        + Guest(name : String, phone : String)
        + Guest(name : String)
        + getName() : String
        + getPhone() : String
        + setName(name : String) : void
        + setPhone(phone : String) : void
        + changePhone(newPhone : String) : void
        + toString() : String
        + equals(o : Object) : boolean
        + hashCode() : int
    }

    class Room {
        - number : int
        - price : double
        + Room(number : int, price : double)
        + Room(number : int)
        + getNumber() : int
        + getPrice() : double
        + setNumber(number : int) : void
        + setPrice(price : double) : void
        + changePrice(newPrice : double) : void
        + isExpensive() : boolean
        + toString() : String
        + equals(o : Object) : boolean
        + hashCode() : int
    }

    class Reservation {
        - guest : Guest
        - room : Room
        - date : String
        + Reservation(guest : Guest, room : Room, date : String)
        + Reservation(guest : Guest, room : Room)
        + getGuest() : Guest
        + getRoom() : Room
        + getDate() : String
        + changeDate(newDate : String) : void
        + toString() : String
        + equals(o : Object) : boolean
        + hashCode() : int
    }

    class Hotel {
        - name : String
        - city : String
        - rooms : List<Room>
        - reservations : List<Reservation>
        + Hotel(name : String, city : String)
        + Hotel(name : String)
        + getName() : String
        + getCity() : String
        + getRooms() : List<Room>
        + getReservations() : List<Reservation>
        + setName(name : String) : void
        + setCity(city : String) : void
        + renameHotel(newName : String) : void
        + changeCity(newCity : String) : void
        + addRoom(room : Room) : void
        + createReservation(guest : Guest, room : Room, date : String) : void
        + removeReservation(reservation : Reservation) : void
        + toString() : String
        + equals(o : Object) : boolean
        + hashCode() : int
    }

    Reservation o-- Guest : guest
    Reservation o-- Room : room
    Hotel o-- Room : rooms
    Hotel *-- Reservation : reservations
}
@enduml